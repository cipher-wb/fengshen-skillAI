sample_id: 146003779-T
date: 2026-05-09
batch: B-002

# 模板本体没有 SkillConfig 5 硬信号（不是技能），改读模板顶层 metadata + 文件名
template_metadata_seen:
  filename: "SkillGraph_146003779_【模板】技能短长按持续施法模板.json"
  category_dir: "技能模板"
  sub_category: "技能"
  node_count: 58
  segment_id_prefix: "146xxx"
  notes: |
    "短长按持续施法"是 v0.2 [模板系统.md] 不变量 1 引用过的核心模板（B-001 5/5 样本中 30234006 + 30325002 调用此模板）。
    class_counts 含 REGISTER_SKILL_EVENT × 2 + UNREGISTER_SKILL_EVENT × 2 + SHOW_SKILL_BUILD_UP_PROGRESS_BAR_UI × 2 + INTERRUPT_SKILLEFFECT × 1。
    这是 F-5 (SkillEvent 系统) 的核心证据样本之一。

prediction:
  estimated_root_class: "TSET_ORDER_EXECUTE"   # F-1 验证：模板本体的 root 应该也是 ORDER 而非 RUN_TEMPL
  reasoning_root: |
    citing [控制流子系统.md] 不变量 A："5/5 全部触碰，每样本 11~18 次 ORDER"。模板本体 ORDER × 12 是骨架数。
    citing hold-out F-1: "3/3 hold-out 样本 root 都是 TSET_ORDER_EXECUTE"。
    既然 F-1 显示宗门技能 root 是 ORDER，作为它们调用的"超模板本体"，root 应该也是 ORDER（被 RUN_TEMPL 包装时由调用方提供 ORDER 容器，但模板本体内部也需要自己的 ORDER 骨架）。

  estimated_node_count: 58  # corpus_scan 已知
  topology_sketch: |
    flowchart LR
      ROOT[TSET_ORDER_EXECUTE] --> SHOW_UI[SHOW_SKILL_BUILD_UP_PROGRESS_BAR_UI]
      ROOT --> REG[TSET_REGISTER_SKILL_EVENT 短按]
      ROOT --> REG2[TSET_REGISTER_SKILL_EVENT 长按]
      REG --> ORDER_SHORT[ORDER 短按分支]
      REG2 --> ORDER_LONG[ORDER 长按分支]
      ORDER_SHORT --> COND[TSET_CONDITION_EXECUTE]
      ORDER_LONG --> RUN_EFFECT[TSET_RUN_SKILL_EFFECT × 6]
      ROOT --> INTERRUPT[TSET_INTERRUPT_SKILLEFFECT]
      ROOT --> UNREG[TSET_UNREGISTER_SKILL_EVENT × 2]

  expected_subsystems_touched:
    - 控制流子系统 (ORDER × 12 + DELAY × 2 + CONDITION × 5)
    - 模板系统 (本身就是模板)
    - SkillEvent 系统 (REGISTER × 2 + UNREGISTER × 2 — 短按/长按事件)
    - SkillTag 系统 (SkillTagsConfigNode × 2 + GET_SKILL_TAG × 1 + MOD_TAG × 1 — 用于"是否在蓄力"状态)
    - 中断系统 (SkillInterruptConfigNode × 2 + INTERRUPT_SKILLEFFECT × 1)

  expected_finding_F5_concrete: |
    本样本应明确 SkillEvent 系统的运作机制：
    - REGISTER_SKILL_EVENT 注册回调（短按事件 / 长按事件）
    - UNREGISTER_SKILL_EVENT 在退出蓄力时清理
    - 配套 SkillEffectPassiveExecuteInfo 用于"事件触发时跑哪段 effect"

  open_question_to_verify_F1: |
    模板本体 root 是 ORDER 还是 RUN_TEMPL？
    若是 ORDER → F-1 假说"宗门技能 root 也是 ORDER"得到二阶证据（模板↔调用方 root 一致）
    若是 RUN_TEMPL → F-1 假说被推翻，模板和宗门技能 root 类型不一致需另解
