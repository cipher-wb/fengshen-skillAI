---
sample_id: 146003779-T
date: 2026-05-09
batch: B-002
sample_score: 0.875
4d_breakdown: 节点 1.0 / 拓扑 1.0 / 模板 N/A (本身是模板) / 子系统 0.5
---

# Diff: 146003779-T 持续施法模板本体

## Prediction → Actual 对比

| 维度 | Prediction | Actual | 命中度 |
|------|------------|--------|--------|
| Root 类 | TSET_ORDER_EXECUTE | TSET_ORDER_EXECUTE (rid=1000, IsTemplate=True, ID=146003779) | ✓ 1.0 |
| 节点数 | 58 | 58 | ✓ 1.0 |
| 顶层骨架 | ORDER → SHOW_UI / REG×2 / RUN_EFFECT×6 / INTERRUPT / UNREG×2 | 完全命中（root 6 Params + REG 2 + UNREG 2 + INTERRUPT 1 + SHOW_UI 2） | ✓ 1.0 |
| 子系统列表 | 控制流 / 模板 / SkillEvent / SkillTag / 中断 | 命中（仅"中断"在 v0.2 不算独立子系统，可不扣分） | 0.5 |

**sample_score = (1.0 + 1.0 + N/A + 0.5)/3 = 0.875**

## 核心新发现（4 条）

### 教训 1（F-1 验证 ✓）：模板本体 root = ORDER_EXECUTE，IsTemplate=True 字段是判定标记

rid=1000 的 ORDER_EXECUTE 节点，data 字段含 **IsTemplate: True** + **TemplateParams 数组（10 项）** + **TemplateParamsDesc** + **TemplateParamsCustomAdd**。
**这是判定一个节点是否"模板本体接口节点"的硬信号字段**。

证据：
- rid=1000 IsTemplate=True，ID=146003779（与文件名 SkillGraph_146003779 完全一致 → "模板入口 ID = 文件 ID"）
- 同 JSON 内还有 11 个 ORDER_EXECUTE 节点，全部 IsTemplate=False（模板内部使用的普通 ORDER）

**对 v0.2 [模板系统.md] 不变量 1 的二阶证据**：
- 不变量 1 原文："超模板可以是技能唯一 root"——本批确认**模板本体的 root 也是 ORDER**，不是 RUN_TEMPL
- 综合 hold-out F-1（3/3 宗门技能 root 也是 ORDER），可以下结论：**root 永远是 ORDER 或 ORDER 系（DELAY、SWITCH、CONDITION），不会是 RUN_TEMPL**
- 更精确的不变量应改写为："**模板系统通过节点上的 IsTemplate=True 字段标识，模板 root 仍然是 ORDER；调用方通过 RUN_SKILL_EFFECT_TEMPLATE 节点指向某 SkillEffectConfig.ID（模板 ID）来引用模板**"

### 教训 2（F-2 段位字典）：14xxx 段位是"通用骨架/技能模板"集合

146003779 的 SkillEffectType=1（**SkillEffectType 这个字段在 ConfigJson 内**，不是节点类，是底层 effect type 的分类号）。
本样本 14xxxxxxx 段含**模板入口 + 同段位的子模板**：
- 146003779（root） → 146003827（蓄力状态）/ 146003819 / 146004734 / 146010046 / 146003788 / 146003784（6 个子 effect_id）
- 146003789、146003794、146003791... 等都是同段位的"子 effect/子模板/子节点"

**修订建议**：v0.2 [模板系统.md] 不变量 2 中"14xxxxxxx 段：持续施法 / 神通骨架"过窄，应改为"**14xxxxxxx 段：通用功能/技能/状态模板大段（含模板入口 + 模板内部子 effect）**"。该段不止"持续施法"，可能含数十个子类。

### 教训 3（F-5 SkillEvent 系统建页 ✓✓✓）：完整字段语义

**REGISTER_SKILL_EVENT 字段语义**（基于 rid=1018/1019 ConfigJson.Params 11 项）：
- **Param[0]** = 事件 ID（41 / 42 — 推测 41=释放、42=中断 / 短按 / 长按之一）
- **Param[1]** = 回调 effect_id（注册成功后挂这个 effect）
- **Param[4]** = 配套 SkillTag ID（1460461 / 1460462 — 用于事件触发的标记 tag）
- **Param[5-6]** = 触发次数 / 触发模式（推测）

**UNREGISTER_SKILL_EVENT 字段语义**（基于 rid=1040/1041）：
- **Param[0]** = 事件 ID（与注册一致）
- **Param[1]** = 注册时的 effect_id（用于反查槽位）

**与 SkillEffectPassiveExecuteInfo 的关系**：
- 持续施法模板的"短按事件"和"长按事件"通过本机制实现，本身不依赖 SkillConfig.SkillEffectPassiveExecuteInfo
- **推论**：SkillEvent 系统是模板内部使用的"局部事件订阅机制"；SkillEffectPassiveExecuteInfo 是技能层面的"全局被动入口"
- 二者**并行存在**——SkillEvent 在 effect 内部链路，SkillEffectPassive 在 SkillConfig 顶层

### 教训 4（F-2 反例确认）：38000289 不是模板 ID

corpus_scan 全 3571 样本中**没有任何 SkillGraph_38000289 文件**。
B-001 D-1 中"30124001 → RUN_TEMPL 38000289（连招 2 段）"的描述需要修正：
- 38000289 应该是 **SkillEffectConfig.ID**（effect 级别 ID），不是模板根节点 ID
- 真实"连招 2 段"模板是 `SkillGraph_175_0102【模板】技能连招(2段).json`（**175 段位**才是连招模板）
- 推测 38000289 是 175_0102 模板内部某个子 effect ID，或是某个非模板的 effect 节点

**修订建议**：v0.2 [模板系统.md] 不变量 2 中"38xxxxxxx 段：连招类（典型：38000289 连招 2 段模板）"是**直接错误**，应改为"**真实连招模板段是 175 段（175_0101~0105 = 1~5 段连招模板）**"，38000289 暂时归为"非模板的 SkillEffectConfig，含义待验证"。

## ATTRIBUTE: F-1 ~ F-6 验证结果

- **F-1（root 是 ORDER 而非 RUN_TEMPL）**：✓ **强证实** — 模板本体的 root 也是 ORDER，与 hold-out 3/3 宗门技能 root=ORDER 形成二阶证据。原 [模板系统.md] 不变量 1 表述需修订为 "模板内部 root 是 ORDER；调用方通过 RUN_TEMPL 节点引用模板"
- **F-2（段位字典）**：✓ **新证据** — 14xxx 段是"通用模板大段"含入口 + 子 effect；38xxx 段位**根本不存在于模板分类**（175 才是连招模板段）；hold-out F-2 推测的 19xxx/22xxx 待 sample 5 验证
- **F-5（SkillEvent 系统）**：✓ **建页就绪** — REGISTER + UNREGISTER + Event ID 41/42 + Tag 配套 1460461/1460462 + INTERRUPT_SKILLEFFECT 联动构成完整子系统

## 价值（对 mental_model 的 delta 提案）

1. **修订 [模板系统.md] 不变量 1**：从"超模板可以是技能唯一 root"改为"模板内部 root 仍是 ORDER；调用方通过 RUN_TEMPL 节点引用模板入口（IsTemplate=True 节点）"
2. **修订 [模板系统.md] 不变量 2**：38xxx 段位错，改为 14xxx + 175（连招）+ 186xxx（功能）+ 19xxx 等多段
3. **新建 [SkillEvent系统.md]**：含 REGISTER/UNREGISTER 字段语义 + Event ID 41/42（待 5 样本验证 ID 字典）
4. **新增不变量**：节点 data.IsTemplate=True 是"模板入口节点"硬信号，配合 TemplateParams 数组可识别模板的对外参数面板

## 引用源码

- 节点 rid=1000 (`TSET_ORDER_EXECUTE`, IsTemplate=True) — 模板入口
- 节点 rid=1018/1019 (`TSET_REGISTER_SKILL_EVENT`) — Event ID 41/42
- 节点 rid=1040/1041 (`TSET_UNREGISTER_SKILL_EVENT`) — 与 1018/1019 配对
- 节点 rid=1039 (`TSET_INTERRUPT_SKILLEFFECT`) — Param[2]=1460056 中断标记 tag
