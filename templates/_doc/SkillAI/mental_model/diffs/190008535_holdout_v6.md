# 190008535 hold-out v0.6 DIFF

**focus**: 样本 2 - D-508 §0 非技能文件识别决策树测试（避开 28xxx 段位）

## 真值（开卷）

- node_count: 38（与 corpus_scan 一致）
- **SkillConfigNode: 0 个** ⭐
- **IsTemplate=True 节点存在**（在 TSET_ORDER_EXECUTE rid 上 ID=190008535）⭐
- 顶层无 SkillConfig.SkillEffectExecuteInfo / Passive
- → **类别 2 模板本体（Mode D）**
- BulletConfig=1 (1900306) | ADD_FORCE=1 | SET_COLL=3 | CREATE_ROLE=1 | DESTROY=2 | DELAY=4 | ORDER=5 | REPEAT=2
- RUN_TEMPL=2：调用模板 **38000236（位移_朝速度方向）+ 186004802（受击施法效果）**
- 额外节点：ModelConfig × 2 / FollowEntity / VIEW_OPTION / ADD_BUFF + BuffConfig × 1 / Skill_Select_Circle / NUM_CALCULATE / CREATE_RANDOM_NUM × 2

## 预测 vs 实际

| 维度 | 预测 | 实际 | 评分 |
|------|------|------|-----|
| 节点数 | 38 | 38 | **1.0** |
| 拓扑 | Mode D 模板型 + ORDER root + SkillSelect + CreateRole + ADD_FORCE + DELAY × 4 + bullet + SET_COLL × 3 + REPEAT × 2 + DESTROY × 2 + RUN_TEMPL × 2 | 全部命中 + 额外 ModelConfig × 2 / FollowEntity / VIEW_OPTION / ADD_BUFF + BuffConfig | **0.875** |
| 模板 | 175xxx / 1900164xx / 44xxx | 实际 **38000236（38xxx 段，v0.5 D-402 已知）+ 186004802（186xxx 段，v0.5 已知）** | **0.5**（段位字典命中 38xxx + 186xxx，但具体未预测到 — 36000236 / 186004802 是 v0.6 字典内段位但具体 ID 未命中）|
| 子系统 | SkillEntry / 模板 / 控制流 / 实体碰撞 / 子弹 5 个 | 全部命中 + **Buff（ADD_BUFF + BuffConfig × 1）漏 1 个** | **0.875** |

**sample_score = (1.0 + 0.875 + 0.5 + 0.875) / 4 = 0.8125**

## D-508 KPI（核心）

| KPI | 预测 | 实际 | 命中 |
|-----|------|------|-----|
| 类别识别 | 类别 2 模板本体（IsTemplate=True） | IsTemplate=True 在 ORDER root 上 ID=190008535 | ✓ |
| Mode 推断 | 模式 D 模板型（不属于 5 类） | 模式 D 模板型 | ✓ |
| 闭卷决策树 | skill_sub_type=null + "【模板】"前缀 + 关卡模板目录 → 类别 2 | 决策树路径完全正确 | ✓✓ |

**重大成功**：v0.5 hold-out 时若闭卷遇到此样本，AI 大概率强行套 5 类入口模式预测 = drift；**v0.6 D-508 决策树 100% 修复了这个 drift 风险，且能在新段位（1900xxx）和新目录（关卡模板）上泛化**——决策树并未仅依赖 28xxx 段位匹配。

## drift 风险归因

### 真 drift 信号：无

无 drift 复发。D-508 决策树在字典外段位 + 字典外目录上完美工作。

### 非 drift（认知保持稳定）

- 类别识别决策树字典外段位泛化（**1900xxx ≠ 28xxx，仍正确识别**）
- IsTemplate=True 信号识别正确
- 模板段位 38xxx + 186xxx 是 v0.5 字典内（D-402 + D-306）— 段位字典稳健
- 1900xxxxx 段位族确实是模板段位族（与 v0.4 D-401 三联包 1900164xx 同段位族）

### 小 drift：模板系统 D-503 三联包未命中此样本

预测 1900164xx 三联包候选，实际 190008535 自身就是 1900xxx 段位**模板本体**，**没调三联包，反而是被三联包同段位族**——这印证了 1900xxx 是"功能模板段位族"的整体认知，但单个样本是否调三联包是不固定的。

## 子系统页延伸

### SkillEntry 系统（D-508 印证）

- **§0 非技能文件识别清单**：第 3 个印证样本（除 28023113 / 28017851 外，**字典外段位 190008535 类别 2 模板本体**第 3 印证）
- 决策树跨段位泛化 KPI ✓✓

### 模板系统

- §SkillEditor 文件三分类 — 类别 2 模板本体在 1900xxx 段位族（关卡模板目录）的第 1 印证（v0.6 之前类别 2 印证主要在 14xxx / 175xxx 段）

## 引用

- 真值文件：`{{SKILLGRAPH_JSONS_ROOT}}关卡模板/SkillGraph_190008535_【模板】触发触发主攻击.json`
- 预测：`predictions/190008535_holdout_v6.yaml`
