# Hold-out Diff: 30531006 【金宗门】标签效果-连击

**Predicted with mental_model_version: v0.2** (hold-out 验证)

## Prediction (closed-book v0.2)

参考 [predictions/30531006.yaml](../predictions/30531006.yaml)。

- 节点数：90（实际 85）
- 拓扑：单 Passive root → ENTITY_SUB_TYPE 检查 → BD_LABEL 检查 → tag 累加 → switch 多分支
- 模板：不套大模板，主要 flow 内自写
- 子系统：SkillEntry（Active=0 反例）+ 控制流 + SkillTag（核心）+ Buff + 模板
- 关键预测：**Active=0 + Passive only** 是 v0.2 [SkillEntry系统.md] 不变量 1 反例

## Actual

- 实际节点数：**85**（预测 90，差 5.88% → ±20% 内 = 1.0）
- 实际拓扑：
  - **Active=0** 命中（仅 Passive root）
  - **Passive root = `TSET_ORDER_EXECUTE` (rid=1000)** ← 不是 RUN_TEMPL
  - 描述："**30%概率触发额外100%伤害**" ← 验证"被动触发型"判断
  - decompiler `flow: []` 因为没 Active 主流程
  - 完整的 5 SkillTagsConfigNode（其中 5 个 tag 含义已显式声明）：
    - 2250047 "金系连击-子类型值"
    - 2250049 "金系连锋技能当前触发帧"
    - 2250036 "基础连击概率增加"
    - 2250122 "记录连击触发动态概率"
    - 2250123 "标记本次攻击是否可触发"
- 模板 ID：`190016404` ×1 / `190016485` ×1 / `190016523` ×1 / `225003230` ×1（22 段位新发现）
- Lint：1 error（**E019 GET_SKILL_TAG_VALUE 读 tag_id=2250113 未声明**，**真 cross-skill 读** —
  与 v0.2 [SkillTag系统.md] 不变量 B Pattern A 一致）+ 68 warn

## 4 维差异打分

| 维度 | Prediction (v0.2) | Actual | Score | 备注 |
|------|------|------|------|------|
| 节点数 | 90 | 85 | **1.0** | 5.88% 偏差 |
| 拓扑同形 | 单 Passive root（ORDER 类）+ ENTITY_SUB_TYPE 分类 + BD_LABEL 检查 + tag 累加 + switch 分支 | 单 Passive root = ORDER + ENTITY_SUB_TYPE×3 + BD_LABEL×2 + 6 MOD_TAG + 1 SWITCH | **1.0** | **完美命中** — Passive only / ORDER root / 多重条件树 / SWITCH 分支 / tag 累加 全部对 |
| 模板 | 不套大模板（RUN_TEMPL=4 < 5 阈值） | 4 个独立 RUN_TEMPL（19xxxxxxx + 22xxxxxxx），无单一主模板 | **1.0** | 模板使用模式预测对 |
| 子系统 | SkillEntry（反例）+ 控制流 + SkillTag（核心）+ Buff + 模板 | SkillEntry（Active=0）+ 控制流（SWITCH/AND/OR/COMPARE）+ SkillTag（5 declared + 1 cross-skill）+ Buff（2 ADD/1 REMOVE）+ 模板 + **SkillEvent 系统**（REGISTER_SKILL_EVENT×2 + UNREGISTER）| **0.5** | 漏掉 **"SkillEvent 系统"**（注册/反注册技能事件机制），这是被动触发型技能的关键 |

**sample_score = (1.0 + 1.0 + 1.0 + 0.5) / 4 = 0.875**

## 关键命中

1. **节点数 1.0**：corpus_scan 完整后预测精度高。
2. **拓扑完美命中**：v0.2 [SkillEntry系统.md] 不变量 1 反例 + [控制流子系统.md] 5 控制原语
   + [SkillTag系统.md] 多源声明，三个子系统页协同推理出来。
3. **Active=0 反例验证**：v0.2 [SkillEntry系统.md] 不变量 1 "Active 入口必填" 在
   "宗门标签型"技能上不成立，**这是 hold-out 阶段最强的认知补丁**。
4. **真 E019 cross-skill 读**：[SkillTag系统.md] 不变量 B Pattern A
   "技能级 Param[1]≠0" 模式，本样本 GET tag 2250113 是真跨技能读，lint 报警是 true positive。
5. **不套大模板预测对**：v0.2 [模板系统.md] 反直觉 1 阈值"≥ 5 才怀疑"在此样本验证。
   RUN_TEMPL=4 < 5 阈值 → 不套大模板，多个独立小模板调用。

## 关键漏判

1. **漏掉 SkillEvent 系统**：`TSET_REGISTER_SKILL_EVENT` × 2 + `TSET_UNREGISTER_SKILL_EVENT` × 1
   是被动触发型技能的核心机制，v0.2 心智模型完全没建这个子系统页。
2. **22xxxxxxx 段位**：又一个新段位（225003230）。

## 归因到子系统认知

- delta-A：**[SkillEntry系统.md] 不变量 1 修订** — "Active 入口必填" 应改为
  "Active 入口在主动施放型技能必填，但'宗门标签型/被动触发型'技能可单 Passive
  （Active=0）"。30531006 是确认证据。
- delta-B：**新子系统候选**：**"SkillEvent 系统"**
  （TSET_REGISTER_SKILL_EVENT / UNREGISTER_SKILL_EVENT），被动触发型技能的核心机制。
  v0.2 没建页，建议下次 batch 优先建。
- delta-C：**[控制流子系统.md] 强化** — TSCT_AND / TSCT_OR / TSCT_ENTITY_SUB_TYPE /
  TSCT_IS_SKILL_CONTAIN_BD_FIRST_LABEL / TSCT_IS_ELEMENT 等条件树原语在被动触发型样本中
  密集使用，[控制流子系统.md] 仍不确定 1 应升级为正式条目。
- delta-D：**[模板系统.md] 段位扩展再确认** — 22xxxxxxx 段（225003230）出现，
  与 30222002 / 30133001 的 19xxxxxxx / 18xxxxxxx 一起，说明编辑器至少 5+ 段模板。
