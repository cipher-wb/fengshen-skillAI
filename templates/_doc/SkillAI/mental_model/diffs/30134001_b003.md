# DIFF 30134001【火系】天阶功法-普攻（B-003 / 攻击盲点 4：ADD_BUFF 无 BuffConfigNode）

**预测来源**：[predictions/30134001.yaml](../predictions/30134001.yaml)
**心智版本**：v0.3
**预期攻击**：Buff 系统 不变量 1（v0.3 写"BuffConfigNode 是 buff 实体定义"，但本样本 3 ADD_BUFF + 0 BuffConfig）

## 关键事实（READ 真值）

| 字段 | 真值 | 预测 | 命中 |
|------|------|------|------|
| 节点数 | 110 | 110 | ✓ |
| CdType | 4 | 4（连招型） | ✓ |
| CdTime | 21 | 21 | ✓ |
| SubType | 101 | 101（v0.3 字典外） | ✓ 字典需扩补 |
| Active.SkillEffectConfigID | **190015697** | 非0 | ✓ |
| Passive.SkillEffectConfigID | **0** | 推测可能 ≠0 | ✗ Mode A 实际 |
| 入口模式 | **A 纯 Active** | B 双 root | ✗ 部分错（普攻不必有 Passive） |
| EnhanceSkillBuffConfigID | 0 | — | — |
| ADD_BUFF count | 3 | 3 | ✓ |
| BuffConfigNode count | **0** | 0（核心反例） | ✓ |
| RUN_TEMPL count | 16 | 16 | ✓ |

## 关键发现（DIFF）

### 发现 1：盲点 4 强证 — ADD_BUFF 引用外部 BuffConfig 表（不需要 flow 内节点）

3 个 ADD_BUFF 全部引用 **buff_id=440501**（同一 buff，本技能内 3 次 ADD_BUFF 触发同一 buff 加层）：

| ADD_BUFF rid | buff_id_param2 | flow 内 BuffConfig？ |
|------|---------|------|
| 1029 | 440501 | ✗ 全 flow 0 BuffConfigNode |
| 1035 | 440501 | ✗ 同上 |
| 1042 | 440501 | ✗ 同上 |

**Params 结构**：[2, 3, 440501, 0, 0, 1, 1] — 与其他样本 ADD_BUFF Params 同构（7 项）。

→ **强证 v0.3 Buff 系统 不变量 1 修订**：
1. ~~"BuffConfigNode 是 buff 实体定义"~~（v0.3 写法太强）
2. **正确语义**：
   - **`TSET_ADD_BUFF.Params[2].Value` = 全局 BuffConfig 表 ID（运行时寻址）** — buff 真实定义在 Excel 表 `Buff/BuffConfig.xlsx`
   - **`BuffConfigNode`（flow 内）= 编辑器侧的 buff 元数据快照 / 调试占位**（含 BuffNameKey / EffectID / Icon / IsShowIcon 等编辑器字段）
   - flow 内 BuffConfigNode **可选**：本技能新建的 buff 通常会加（方便编辑器 inspector 查看）；引用其他技能/通用 buff 不必加节点
3. 三阶证据链：
   - 30134001（B-003）：3 ADD_BUFF + 0 BuffConfig（全外部引用 440501）
   - 30531008（B-003）：5 ADD_BUFF + 3 BuffConfig（部分内部 + 部分外部 2250124 / 2250102 / 2250091 内 / 10000 外）
   - 30214004（hold-out v0.3）：4 ADD_BUFF + 0 BuffConfig（全外部）

### 发现 2：普攻不一定有 Passive（修订 SkillEntry 反直觉点 1）

**v0.3 SkillEntry 系统反直觉点 1**："普攻也有 Passive" — 30124001 印证（Active=220001248 / Passive=220004066）
**本样本反例**：30134001 Mode A 纯 Active（Active=190015697 / Passive=0）

→ **v0.3 反直觉 1 需精化**："普攻**可能有** Passive，不是必有"。
- 30124001（火地阶火球普攻）= Mode B 双 root
- 30134001（火天阶功法-普攻）= Mode A 纯 Active

可能的判别：天阶功法-普攻 = 自身机制完整（连招 + buff 自循环），不需要被动响应；地阶普攻命中后触发额外效果（Passive）。
→ **入口模式与"是否需要被动响应"挂钩，与"普攻 vs 神通"分类无关**。

### 发现 3：SubType 字典扩 101（v0.3 字典外）

| SubType | 出处 | 推测 |
|---------|------|------|
| 0 | 30534004 / 30531008 | 标签效果型 |
| 101 | **30134001（B-003 新）** | **天阶功法-普攻 / 连招型普攻** |
| 102 | 30431000 / 30222002 / 30215001 | 奇术（机制不固定） |
| 103 | 30331000 / 30315001 / 30324002 / 30325001（B-003 新 2 个） | 神通 / 大招 |
| 601 | 30535000（B-003 新）| 传承心法 |
| 901 | 30433001 | 身法-水属性 |

→ **SubType 字典从 3 → 6**（新增 101 / 103 / 601；其中 103 在 hold-out 已观察 + B-003 二阶印证；101 / 601 是 B-003 全新发现）

### 发现 4：模板调用 - 19xxxx + 18xxxx + 44xxx 段位混用

16 个 RUN_TEMPL 的 Params[2] 真模板 ID（部分）：
- 190016186 / 190016308 / 190016309 / 190015708 / 190015763 / 190015766（**19xxx 段，v0.3 字典内 18xxx+186xxx 段邻近**）
- 44012815 / 44012823 / 44012832 / 44012834（**44xxx 段，v0.3 D-202 已识别"effect 段"**）
- 186004960（v0.3 字典已知）

→ v0.3 模板段位字典 **18xxx + 186xxx (29 个)** 应改为 **18xxx + 186xxx + 19xxx (~多)**；
44xxx 段不是模板段而是 **effect_id 段**（v0.3 D-202 已识别 38000289 是 effect_id；本样本印证 44xxx 也是）。
**最终段位字典澄清**（B-003 修订）：
- 模板 ID 段位：**14xxx / 175 / 18xxx-186xxx / 19xxx / 22xxx / 28xxx / 66xxx**（30531008 调 66001194 新发现）
- effect_id 段位：**44xxx / 38xxx**

## 命中评分（4 维）

- 节点数：±0% → **1.0**
- 拓扑：Mode 预测错（B → A）扣分；3 ADD_BUFF + 16 RUN_TEMPL 命中 → **0.75**
- 模板：16 RUN_TEMPL 数对，段位字典 80% 命中 → **0.75**
- 子系统：5/6 命中（缺 SkillEvent，但本样本无 REG 也合理）→ **0.875**

**sample_score = 0.844**

## 攻击盲点 4 处置

✓ **强证 v0.3 Buff 系统 不变量 1 修订**（30134001 + 30214004 + 30531008 = 3 个 ADD_BUFF 无 BuffConfig 或不平衡）→ 进入 D-304
✓ **附带证据：SubType 字典扩 101 / 普攻 Mode A 反例**（修订 SkillEntry 反直觉 1）→ 进入 D-308
