# 400019 (法器真诀-纹章叠刃刺) DIFF — B-005 / focus 1C

## 闭卷预测 vs 开卷实际

| 维度 | 预测 | 实际 | 命中 |
|------|------|------|------|
| 节点数 | 141 | ~163 (read 估计) | ✓ ±20% 内 = 1.0（corpus_scan 141） |
| 拓扑 | 单 root Passive → ORDER → REG_EVENT × N → callback 内 SkillTag 检查/Buff/元素属性 | 单 root Passive=44001662 → 22 ORDER + 13 VALUE_COMPARE + 13 GET_TAG + 11 REG_EVENT + 11 AND + 11 SKILL_CONFIG_DATA_COMPARE + 10 UNREG_EVENT + 10 NUM_CALCULATE + 10 GET_BUFF_LAYER_COUNT + 10 ADD_ENTITY_ATTR_VALUE + 6 BuffConfig + 5 SkillTags + 5 ModelConfig + 1 ADD_BUFF + 1 ADD_BUFF_OVERLYING_MAX | ✓ 拓扑核心结构完全对：REG/UNREG 大量 + buff_layer 计层 + 实体属性修改 = 1.0 |
| 模板 | 0 | 0 | ✓ N/A (0/0) = 1.0 |
| 子系统 | SkillEntry + SkillEvent + Buff + SkillTag | 全员 + 元素属性（ADD_ENTITY_ATTR_VALUE × 10 = 强烈的属性增益机制） | ✓ 5/5 = 1.0（4 预期 + 1 边缘印证）|

**sample_score = (1.0 + 1.0 + 1.0 + 1.0) / 4 = 1.0**

## drift 修正关键验证

**入口模式预测：Mode C** ✓ **完全命中**

实际：Active=0 / Passive=44001662 → Mode C 纯 Passive。
SubType=501 内确认 Mode C/E 双模式：400001 (Mode E 双 0 入口) + 400019 (Mode C 纯 Passive)。

## 重大新发现：UNREG/REG 平衡的"反复模式"超规模化

| 指标 | 30525001 (v0.5 hold-out) | 400019 (B-005) |
|------|------------------------|---------------|
| REG event_id | 47 × 1 | 15 × 5, 11 × 5, 4 × 1 |
| UNREG event_id | 47 × 1 | 15 × 5, 11 × 5 |
| REG/UNREG 平衡 | ✓ | ✓ 完美平衡（10/10 + 1 单 REG） |

**v0.4 D-404 / B-004 D-404 提到的"UNREG 反复模式"**在 400019 完全实例化：5 对 REG/UNREG 同 event_id=15 + 5 对 REG/UNREG 同 event_id=11 + 1 单 REG event_id=4。
这是**事件订阅的批量管理范式**——BuffConfigNode × 6（同时 6 个 Buff 各自管 callback 时机）+ 11 SKILL_CONFIG_DATA_COMPARE（按技能动态切换处理逻辑）。
**法宝/装备型被动当层数变化时动态注册/反注册大量事件**，是新的"super-pattern"。

## 子系统认知对账

| 子系统 | 预测理解 | 实测印证 | 是否更新认知？ |
|--------|---------|---------|-------------|
| SkillEntry | Mode C 纯 Passive | ✓ | **D-501 加强**：SubType=501 内 Mode C/E 并存 |
| SkillEvent | REG/UNREG callback 驱动 | ✓ 11 REG + 10 UNREG（接近完美平衡） | **D-505**：UNREG 反复模式从 v0.5 单观察升级为"批量动态订阅管理范式"，候选 §UNREG 字段语义段落补充 |
| Buff | 装备/法宝典型 | ✓ 6 BuffConfig + 1 ADD_BUFF + 1 ADD_BUFF_OVERLYING_MAX（叠加上限管理）| **D-506**（弱）：TSET_ADD_BUFF_OVERLYING_MAX 是 buff_layer 之外的"叠加上限"原语，候选 Buff 系统§字段语义补充 |
| SkillTag | GET 13 + 5 SkillTagsConfigNode 三声明源协作 | ✓ | 无新增 |
| 元素属性子系统（候选） | 未显式预测 | ADD_ENTITY_ATTR_VALUE × 10 + MODIFY_ENTITY_ATTR_VALUE 等密集使用 | **B-002 D-211 暂存档候选**有第 2 样本印证（30534004 后第 2 个），可考虑升级 |

## 新值

- **TSCT_SKILL_CONFIG_DATA_COMPARE × 11** — "技能配置数据比较"条件原语，未在 mental_model 提及。预期是用 SkillConfig 字段做条件判断（如按 buff_layer 数值范围分支处理）。**控制流子系统候选扩补**（中信号 1/5，结合 30510007 的 TSCT_CHECK_SKILL_SLOT_TYPE × 3 形成"条件子系统"候选）。
- **TSET_ADD_BUFF_OVERLYING_MAX × 1** — "Buff 叠加上限设置"原语，用户能在运行时改 buff 上限（弱信号 1/5）。

## 归因

- **D-501 加强**：SubType=501 双模式（C+E）锁定。结合 SubType=303（A+E）和 SubType=701（C+E），3 个 v0.4 新增 SubType 全部不锁定单模式。
- **D-505**：UNREG 反复模式升级（11 REG + 10 UNREG 大规模订阅管理）。
- **D-506（弱）**：ADD_BUFF_OVERLYING_MAX 字段语义。
- **隐含强化**：v0.5 D-410 callback effect ID 段位约定（root-同段位私有）—— 400019 root=44001662（44xxx）+ ADD_BUFF id=44005772（44xxx 同段位）印证。
