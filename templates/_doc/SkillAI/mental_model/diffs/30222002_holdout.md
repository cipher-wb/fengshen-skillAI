# Hold-out Diff: 30222002 【木宗门】奇术_地阶_落木遁形

**Predicted with mental_model_version: v0.2** (hold-out 验证)

## Prediction (closed-book v0.2)

参考 [predictions/30222002.yaml](../predictions/30222002.yaml)。

- 节点数：110（实际 108）
- 拓扑：单 Active root → RUN_TEMPL 高阶模板 → 模板内嵌 3 子弹 + Passive 旁路（双 root）
- 模板：38xxxxxxx 段位连招型（B-001 30431000 同 SkillSubType=102 用 38000289）
- 子系统：模板 + 控制流 + SkillEntry + SkillTag + Buff + 子弹

## Actual

- 实际节点数：**108**（预测 110，差 1.85% → ±20% 内 = 1.0）
- 实际拓扑：
  - **Active root = `TSET_ORDER_EXECUTE` (rid=1000)** ← 不是 RUN_TEMPL！
  - **Passive root = `TSET_DELAY_EXECUTE` (rid=1073)** ← 也不是 RUN_TEMPL！
  - **双 root** 命中（与 [SkillEntry系统.md] 不变量 2 一致）
- 模板 ID 实际清单：
  - `190016404` ×4 / `190016485` ×3 / `190016523` ×3（**新段位 19xxxxxxx**）
  - `186003862` ×1 / `186004942` ×1（**新段位 18xxxxxxx**，"施法状态"模板）
  - `38000228` ×1（38 段位有，但不是连招而是别的）
- 真实机制：**"落木遁形"= 真实的位移/隐身**！
  - `TSET_APPLY_ENTITY_EFFECT` "闪烁过程中主角变透明"
  - `TSET_SET_ENTITY_COLLISION_ENABLE_STATE` ×4（关闭碰撞 = 隐身）
  - `TSET_DESTROY_ENTITY` ×1（销毁分身？）
  - `TSET_REPEAT_EXECUTE` "重复3次" + 莲花陷阱子弹（model_id=320035）
  - 这是 **v0.2 [Buff系统.md] 反直觉 3 "身法/奇术 ≠ 位移" 的明确反例**！
- Lint：0 error / 37 warn（W001 desc 空 + W003 急速影响=0）

## 4 维差异打分

| 维度 | Prediction (v0.2) | Actual | Score | 备注 |
|------|------|------|------|------|
| 节点数 | 110 | 108 | **1.0** | 1.85% 偏差 |
| 拓扑同形 | 单 RUN_TEMPL root + 3 子弹模板内嵌 | 双 root（ORDER+DELAY，非 RUN_TEMPL） + 19xxxxxxx 系列散用 | **0.5** | 双 root 命中（[SkillEntry系统.md] 不变量 2 用对了），但 root 类型错（[模板系统.md] 不变量 1 在此样本不成立） |
| 模板 | 38xxxxxxx 连招型 | 19xxxxxxx + 18xxxxxxx（新段位）+ 38000228（非连招） | **0.0** | v0.2 [模板系统.md] 不变量 2 段位语义错——只识别 14/38 两段，漏 19/18/22 等 |
| 子系统 | 模板/控制流/SkillEntry/SkillTag/Buff/子弹 | 控制流/SkillEntry/SkillTag/Buff/子弹/**实体碰撞控制** + **遁形/隐身**机制 | **0.5** | 子系统列对 5/6，**漏掉了"实体碰撞/隐身机制"这个 v0.2 没建的子系统**。这才是落木遁形的核心。|

**sample_score = (1.0 + 0.5 + 0.0 + 0.5) / 4 = 0.50**

## 关键命中

1. **节点数 1.0**：v0.2 corpus_scan class_counts 完整后选样精度提升。
2. **双 root 命中**：[SkillEntry系统.md] 不变量 2 起作用——Passive=32001376 ≠ 0 → 双 root，预测对。

## 关键漏判

1. **核心机制理解错**："落木遁形"实际是**真正的隐身/位移**机制（碰撞关闭 + 透明化），
   不是 v0.2 [Buff系统.md] 反直觉 3 写的"奇术=buff 串联控制效果"。
   → **明确认知冲突**：v0.2 的"奇术不位移" 只在 30431000 一个样本上成立，泛化错了。
2. **新模板段位 19xxxxxxx / 18xxxxxxx / 22xxxxxxx**：v0.2 [模板系统.md] 仍不确定 2
   "14/38 之外还有哪些段位" 的真实答案出现了——3 hold-out 样本都用 19xxxxxxx 系列。
3. **root 类型 ≠ RUN_TEMPL**：v0.2 [模板系统.md] 不变量 1 "超模板可以是技能唯一 root"
   在此样本不成立——root 是 ORDER_EXECUTE，模板只是 flow 内的工具调用。

## 归因到子系统认知

- delta-A：**[Buff系统.md] 反直觉 3 范化错** — "身法/奇术不位移" 仅在 30431000 成立，
  落木遁形（30222002）是反例。修订为"奇术机制因技能而异，可以是 buff 串联，也可以是
  实体碰撞/位移控制"。
- delta-B：**[模板系统.md] 段位字典需扩展** — 新增 19xxxxxxx / 18xxxxxxx / 22xxxxxxx 段位。
- delta-C：**[模板系统.md] 不变量 1 适用范围** — "RUN_TEMPL 可以是唯一 root" 是高节点
  特定模式，不能泛化为所有技能。本样本 root 是 ORDER。
- delta-D：**新子系统候选**：**"实体碰撞与可见性"子系统**（SET_ENTITY_COLLISION_ENABLE_STATE
  / APPLY_ENTITY_EFFECT 透明化 / DESTROY_ENTITY），v0.2 没建页。
