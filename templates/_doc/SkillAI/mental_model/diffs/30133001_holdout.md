# Hold-out Diff: 30133001 【水宗门】招式_天阶（水天阶普攻）

**Predicted with mental_model_version: v0.2** (hold-out 验证)

## Prediction (closed-book v0.2)

参考 [predictions/30133001.yaml](../predictions/30133001.yaml)。

- 节点数：65（实际 62）
- 拓扑：单 root → RUN_TEMPL 38000289（连招 2 段模板）→ 4 段连招 + 4 子弹 + 轻量 buff
- 模板：38000289 连招 2 段（B-001 30124001/30431000 复用）
- 子系统：模板 + 控制流 + SkillEntry（单 root only）+ 子弹 + SkillTag + Buff
- 关键预测：**Passive=0** 是 4/5 大流的反例

## Actual

- 实际节点数：**62**（预测 65，差 4.84% → ±20% 内 = 1.0）
- 实际拓扑：
  - **Active root = `TSET_ORDER_EXECUTE` (rid=1050)** ← 不是 RUN_TEMPL
  - **Passive=0** 命中（单 root）→ [SkillEntry系统.md] 不变量 2 反例验证
  - flow 第一段是 `TSET_CONDITION_EXECUTE` (442030 / 0 / 44014364) — **条件优先**
  - 然后调 RUN_TEMPL → **`38000446`（"SkillGraph_175_0104【模板】技能连招(4段).json"）**
- 模板 ID 实际清单：
  - **`38000446` ×1（4 段连招！不是 2 段）** ← v0.2 段位语义对了一半（38xxxxxxx 是连招）
  - `190016404` ×4 / `190016485` ×4 / `190016523` ×4（**新段位 19xxxxxxx 系列**，每段连招都用）
- Lint：1 error（**E012 招式禁止 CD=0**）+ 33 warn

## 4 维差异打分

| 维度 | Prediction (v0.2) | Actual | Score | 备注 |
|------|------|------|------|------|
| 节点数 | 65 | 62 | **1.0** | 4.84% 偏差 |
| 拓扑同形 | 单 root + 38000289 连招 2 段 + 4 段连招 + 子弹对子 | 单 root（ORDER）+ 38000446 连招 4 段 + 19xxxxxxx 共 12 个内嵌模板调用 | **0.5** | "连招型"命中（CdType=4 推理对），段数推测 4 段对（"4 PLAY_ROLE_ANIM" 命中），但 38000446 ≠ 38000289 |
| 模板 | 38000289（具体 ID） | **38000446** | **0.5** | 段位语义对（38xxxxxxx = 连招）但具体 ID 错。仍比 v0.1 完全无概念好 |
| 子系统 | 模板/控制流/SkillEntry/子弹/SkillTag/Buff（6 个） | 模板/控制流/SkillEntry/子弹/SkillTag/Buff（6 个对齐）| **1.0** | 完全对齐！连"轻量 Buff"都对（实际 1 ADD_BUFF + 1 BuffConfig + 1 HAS_BUFF）|

**sample_score = (1.0 + 0.5 + 0.5 + 1.0) / 4 = 0.75**

## 关键命中

1. **节点数 1.0**：v0.2 corpus_scan 修复后选样精度高。
2. **38xxxxxxx 段位 = 连招**：[模板系统.md] 不变量 2 段位语义化正确——预测 38xxxxxxx 命中，
   只是具体 ID（38000289 vs 38000446）有偏差。
3. **CdType=4 推理对**：[控制流子系统.md] 不变量 D 对应"连招型 + 必有 ComboCdList"。
4. **子系统列表 6/6 对齐**：v0.2 心智地图能够准确识别此类样本涉及哪些子系统。
5. **Passive=0 单 root 推理对**：v0.2 [SkillEntry系统.md] 不变量 2 + 反直觉 1
   "普攻常有 Passive 但也有反例"，本样本是反例命中。

## 关键漏判

1. **root 类型还是 ORDER 不是 RUN_TEMPL**：与 30222002 一样，v0.2 [模板系统.md] 不变量 1
   范化错。RUN_TEMPL 不是技能 root，而是 ORDER 内调用的工具。
2. **新段位 19xxxxxxx**：又一个新段位证据。
3. **lint E012 不是预测中预见的内容**：CD=0 招式型禁止——这条业务规则在 v0.2 [控制流子系统.md]
   CdType 三语义中没体现，相对独立。

## 归因到子系统认知

- delta-A：**[模板系统.md] 不变量 1 修订** — "RUN_TEMPL 可以是唯一 root" 不普遍，多数情况
  root 是 ORDER_EXECUTE，RUN_TEMPL 在 ORDER 内被调用。
- delta-B：**[模板系统.md] 段位语义化方向对** — 38xxxxxxx 段确实是连招型，
  但具体 ID 多样（38000289 / 38000446 / 38000228...）。提议把模板段位识别落到段位级而非具体 ID。
- delta-C：**[模板系统.md] 19xxxxxxx 段位发现** — 19xxxxxxx 是连招内的"小工具模板"，
  在 38xxxxxxx 连招模板内被调用（每段连招都引用 190016404/190016485/190016523）。
  推测：19xxxxxxx 段是"连招通用工具集"。
- delta-D：**[SkillEntry系统.md] 不变量 2 验证** — Passive=0 单 root 普攻样本验证成功。
