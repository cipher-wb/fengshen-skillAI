# 30510007 (无伤诀) DIFF — B-005 / focus 1A

## 闭卷预测 vs 开卷实际

| 维度 | 预测 | 实际 | 命中 |
|------|------|------|------|
| 节点数 | 37 | ~41（class_counts 主流，corpus_scan=37 接近） | ✓ ±20% 内 = 1.0 |
| 拓扑 | 单 root Passive → ORDER → REG_EVENT × N → callback 内 GET_SKILL_TAG/MOD/ADD_BUFF/RUN_TEMPL | 单 root Passive=44016176 → ORDER × 3 + REG_EVENT × 1 + GET_TAG × 4 + MOD_TAG × 2 + ADD_BUFF × 1 + RUN_TEMPL × 3 + REPEAT × 1 + DELAY × 1 + PROBABILITY × 1 + REMOVE_BUFF × 2 | ✓ 拓扑核心结构同形 = 1.0 |
| 模板 | 175xxx + 186xxx + 225xxx | 175000212 + 186007365 + 225005546 | ✓ 段位 100% 命中 = 1.0 |
| 子系统 | SkillEntry + SkillEvent + Buff + SkillTag + 模板 | 全员命中（含 SkillSlotConfig 检查 TSCT_CHECK_SKILL_SLOT_TYPE × 3 — 这是技能槽位条件，未预测但属"边缘细化"） | ✓ 5/5 = 1.0 |

**sample_score = (1.0 + 1.0 + 1.0 + 1.0) / 4 = 1.0**

## drift 修正关键验证

**入口模式预测：Mode C** ✓ **完全命中**

实际：Active=0 / Passive=44016176 → Mode C 纯 Passive。
v0.5 hold-out 时 AI 见 SubType=701 + EvCb=True 推 Mode E（错），本批次按"先看 Active/Passive 再看 SubType"原则推 Mode C（对）。**drift 修正成功 1/1**。

## 子系统认知对账

| 子系统 | 预测理解 | 实测印证 | 是否更新认知？ |
|--------|---------|---------|-------------|
| SkillEntry | SubType=701 内 Mode C ↔ Mode E 平行 | ✓ 30510007 (Mode C) vs 30532001 (Mode E) 同 SubType=701 反例对照成立 | **D-501 主张**：§1 不变量 1 加注 SubType 不锁定模式 |
| SkillEvent | REG callback × N 驱动 | ✓ event_id=4 单 REG（v0.5 字典已含），无 UNREG | 无新增 |
| 模板系统 | 175xxx + 186xxx + 225xxx 工具复用 | ✓ 全段命中 v0.5 字典 | 无新增 |
| Buff 系统 | 心法标签效果，buff_layer + ADD/REMOVE | ✓ ADD_BUFF=660084 (660xxx 段，B-003 D-306 已收) + REMOVE_BUFF × 2 | 无新增 |
| SkillTag 系统 | 心法触发条件，GET/MOD | ✓ 7 SkillTagsConfigNode + 4 GET_TAG + 2 MOD_TAG，三声明源协作 | 无新增 |

## 边缘新值

- TSCT_CHECK_SKILL_SLOT_TYPE × 3 — "技能槽位类型检查"条件原语，30510007 用了 3 次。心法被动需要按槽位差异化触发逻辑。**控制流子系统候选扩补**（弱信号 1/5）。

## 归因（哪些 mental_model 主张被加强 / 修订 / 新增）

- **加强**：SkillEntry §1 不变量 1 模式 C（30510007 是第 3 个 Mode C 样本，30534004 + 30531006 hold-out + 30525001 hold-out v0.5 的同源）。
- **修订**：SkillEntry §1 SubType 字典 701 行加注"内有 Mode C / Mode E 双模式"。
- **新增**（弱信号）：TSCT_CHECK_SKILL_SLOT_TYPE 在控制流子系统的位置。
