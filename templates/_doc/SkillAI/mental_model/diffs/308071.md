# 308071 (青木阵盘技能1) DIFF — B-005 / focus 1B

## 闭卷预测 vs 开卷实际

| 维度 | 预测 | 实际 | 命中 |
|------|------|------|------|
| 节点数 | 221 | ~255 (class_counts 主流值) | ✓ ±20% 内 = 1.0（corpus_scan node_count=221 与 read 估计 ~255 在容差内） |
| 拓扑 | 单 root Active → ORDER → 9 RUN_TEMPL（含三联包 8 + 186 × 1）+ 多段命中/Buff | 单 root Active=44013190 → 27 ORDER + 24 GET_TAG + 12 MOD_TAG + 11 RefConfig + 11 VALUE_COMPARE + 9 RUN_TEMPL（三联包 190xxx × 8 + 186 × 1）+ 7 DELAY + 6 MODIFY_BATTLE_FOLLOW_SHAPE_PARAM + 6 REPEAT + 6 PLAY_ROLE_ANIM + 6 CONDITION + 6 ModelConfigNode + 6 DESTROY_ENTITY + 6 SkillTagsConfigNode + 5 ADD_BUFF + ... | ✓ 三联包 + 9 RUN_TEMPL 完全对，RefConfig/MODIFY_BATTLE_FOLLOW_SHAPE_PARAM 等组件未预测但属于"细节" = 0.875 |
| 模板 | 三联包 190016404+485+523 + 186xxx | 实际：190016523 × 3 + 190016485 × 3 + 190016404 × 2 + 186004942 × 1 = 完全命中 | ✓ 100% = 1.0 |
| 子系统 | SkillEntry + 模板 + SkillEvent + 实体碰撞 + Buff | 全员命中 + 多了"角色动画 / 实体生命周期"（DESTROY_ENTITY × 6 + PLAY_ROLE_ANIM × 6 + FOLLOW_ENTITY × 4 + MODIFY_BATTLE_FOLLOW_SHAPE_PARAM × 6） | ✓ 5/5 = 1.0（边缘扩展不扣分） |

**sample_score = (1.0 + 0.875 + 1.0 + 1.0) / 4 = 0.969**

## drift 修正关键验证

**入口模式预测：Mode A** ✓ **完全命中**

实际：Active=44013190 / Passive=0 → Mode A 纯 Active。
**这是 B-005 最重要的 drift 修正信号**：v0.5 D-403 + D-407 把 SubType=303（法宝召唤激发）写为"双 0 入口（Mode E）共同特征"——完全是过度归纳。
真实情况：SubType=303 内同时存在 Mode A（308071 法宝纯 Active 主动技）+ Mode E（308072 青木天葬 双 0）。

## 关键反例：308071 vs 308072

两者同 SubType=303，同"法宝/本命法宝"目录，同"青木"主题（icon=Skill/GongFa/Mu/skill_mu_302101），却是**完全不同的入口模式**：

| 字段 | 308071 (Mode A) | 308072 (Mode E, B-004 已学) |
|------|-----------------|----------------------------|
| Active root | 44013190 | 0 |
| Passive root | 0 | 0 |
| RUN_TEMPL | 9 (三联包 8 + 186) | 5 (190xxx 集中) |
| RefConfigBaseNode | 11 (高频) | 高频 |
| 节点数 | 221 | (B-004 已学) |
| 入口模式 | A | E |

**结论**：法宝技能（包括青木宗本命法宝）入口模式不锁定 — Mode A 和 Mode E 共存。

## 子系统认知对账

| 子系统 | 预测理解 | 实测印证 | 是否更新认知？ |
|--------|---------|---------|-------------|
| SkillEntry | Mode A 纯 Active | ✓ | **D-501 主张加强**：SubType=303 双模式（A+E），全 9 SubType 都需重审是否锁定模式 |
| 模板系统 | 三联包 D-401 完整复现 | ✓ 8/9 RUN_TEMPL 是 190xxx 三联包，单段命中 = 1 轮 三联包重复 = 多段命中（pick4 30221120 三联包 × 3 印证） | **D-503**：三联包"成组出现率"加强（B-004 已记 §模板组合模式），跨 SubType 跨类别（宗门/法宝/心法均有） |
| SkillEvent | callback 链 | ✓ event_id=1 单 REG（v0.5 字典已含） | 无新增 |
| 实体碰撞与可见性 | IndicatorType=2 直线指示器 | ✓ + DESTROY_ENTITY × 6 + FOLLOW_ENTITY × 4 + MODIFY_BATTLE_FOLLOW_SHAPE_PARAM × 6 | **D-504**：法宝召唤实体的"召唤+消灭"管理范式 — 该子系统候选新增 §"召唤实体生命周期"段（弱信号 1/5） |
| Buff | 法宝典型 | ✓ ADD_BUFF × 5 (440509/440517/440530/440510/10008) + 5 BuffConfig 配对 | 无新增 |

## 归因

- **D-501 加强**：SubType=303 内 Mode A/E 并存（与 SubType=701 内 Mode C/E 并存镜像），任何 "SubType=X → 入口模式 Y" 强映射都是错的。
- **D-503**：三联包跨技能类别（宗门技能/法宝/心法）使用，进一步加强"命中模板套件"普适性的认知。
- **新候选 D-504**：法宝召唤实体的"召唤实体生命周期"——尚需更多样本印证（B-006+ 候选）。
